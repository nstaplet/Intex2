@*@{
    Layout = "_Layout";
}*@

@if (User.Identity.IsAuthenticated & User.IsInRole("Admin") || User.Identity.IsAuthenticated & User.IsInRole("Researcher")) 
{
    Layout = "_AdminLayout";
}
else
{
    Layout = "_Layout";
}